# LeetCode 236 — Lowest Common Ancestor of a Binary Tree
# Pattern: Post-order DFS recursion
# Date: 2026-05-28
# Mode: Teaching @ Start (Hadriel-guided redo)

# Definition for a binary tree node.
class TreeNode:
    def __init__(self, x):
        self.val = x
        self.left = None
        self.right = None


class Solution:
    def lowestCommonAncestor(self, root: 'TreeNode', p: 'TreeNode', q: 'TreeNode') -> 'TreeNode':
        # TODO(human): Blueprint phase — transcribe Step 3 plan into numbered comments BELOW.
        # 5 sections (no code yet, comments ONLY):
        #   1. Null check (base case)
        #   2. Self-match check (descendant-of-itself rule)
        #   3. Recurse left
        #   4. Recurse right
        #   5. Combine results → decide what to return
        #
        # ↓↓↓ Write your numbered blueprint comments here ↓↓↓

        pass
